<div class="row center-xs">
  <div class="col-sm-5">
    <div class="card payment-plan">
      <h2>Freelancer 🤓</h2>
      <p class="light">Unlimited projects, up to 5 users</p>
      <div class="options">
        {{payment-plan-option choosePlan=(action "choosePlan") plan=plans.freelancer.monthly text="Cancel anytime" selectedPlan=selectedPlan}}
        {{payment-plan-option choosePlan=(action "choosePlan") plan=plans.freelancer.annually text="Save 16%" selectedPlan=selectedPlan}}
      </div>
    </div>
  </div>
  <div class="col-sm-5">
    <div class="card payment-plan">
      <h2>Agency 🤓🤓🤓</h2>
      <p class="light">Unlimited everything</p>
      <div class="options">
        {{payment-plan-option choosePlan=(action "choosePlan") plan=plans.agency.monthly text="Cancel anytime" selectedPlan=selectedPlan}}
        {{payment-plan-option choosePlan=(action "choosePlan") plan=plans.agency.annually text="Save 16%" selectedPlan=selectedPlan}}
      </div>
    </div>
  </div>
</div>
<div class="row around-xs">
  <div class="col-xs-12 col-sm-10">
    <div class="card">
      <h1>Billing Information 💳</h1>
      <section>
        {{#credit-card form="#payment-form"}}
          <div class="payment-form-wrapper">
            <form id='payment-form' class='payment-info'>
              <div class="ui input">
                <label>Credit Card Number</label>
                {{input type="text" value=creditCard.number name='number' placeholder='Credit Card Number'}}
              </div>

              <div class="ui input">
                <label>Name on Credit Card</label>
                {{input type="text" value=creditCard.name name='name' placeholder='Name on Credit Card'}}
              </div>

              <div class="ui input exp">
                <label>Expiration</label>
                {{input type="text" name='expiry' value=expDate placeholder='MM/YY'}}
              </div>

              <div class="ui input cvc">
                <label>CVC</label>
                {{input type="text" name='cvc' value=creditCard.cvc placeholder='CVC'}}
              </div>
            </form>

            <div class="total">
              {{#if selectedPlan}}
                <span class="label">
                  <p>{{selectedPlanName}}</p>
                  <p class="small">billed {{selectedPlanCycle}}</p>
                </span>
                <span class="amount">
                  <h1>{{selectedPlanPrice}}</h1>
                </span>
              {{/if}}
            </div>
            {{#spin-button action=(action "upgrade") class="btn-primary" buttonStyle="contract"}}Choose plan{{/spin-button}}
            <p class="small">🔒 processed securely through Stripe</p>
          </div>

        {{/credit-card}}
      </section>

      <section>
        {{error-message errors=cardErrors}}
      </section>

    </div>
  </div>
</div>
